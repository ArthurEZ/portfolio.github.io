function getBotResponse(input) {
    if (input == ""){
        return "text something!";
    }
    else {
        return "เนื่องจากผู้สร้างเจอปัญหาเมื่อผมนำ Web-app ตัวนี้ไป ปล่อยบน domain pythonanywhere ผลปรากฏว่า Chatbot นั้นไม่มีการตอบสนอง ในทางกลับกันหากใช้ domain local กลับใช้งานได้ปกติ สามารถลองใช้ด้วยการเปิด Domain local ได้ที่ลิงค์นี้เลย --> https://github.com/ArthurEZ/E-portfolio";
    }
}